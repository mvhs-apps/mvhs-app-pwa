language: node_js
cache: yarn
dist: precise
node_js:
  - "8"
install:
  - yarn install
  - yarn global add firebase-tools
script:
  - yarn run build:web
after_success:
  - if [ "$TRAVIS_BRANCH" = "master" ]; then firebase deploy --token $FIREBASE_TOKEN; fi